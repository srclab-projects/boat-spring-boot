plugins {
  id 'build.jar'
}

description = "Boat Spring Boot Starter for Test"

java {
  registerFeature('optionalSupport') {
    usingSourceSet(sourceSets.main)
  }
}

dependencies {
  api project(":boat-spring-boot-test")
}