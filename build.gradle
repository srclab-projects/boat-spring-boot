description = "Easy for Spring Boot - Build"

ext {
    dependenciesPlatformGradle = "${project.rootDir}/buildSrc/dependencies-platform.gradle"
    commonDependencies = "${project.rootDir}/buildSrc/dependencies-common.gradle"
    autoconfigureDependencies = "${project.rootDir}/buildSrc/dependencies-autoconfigure.gradle"
    testDependencies = "${project.rootDir}/buildSrc/dependencies-test.gradle"

    deployGradle = "${project.rootDir}/buildSrc/deploy.gradle"
}

allprojects {
    group "xyz.srclab.spring.boot"
    println("Configure ${project.group}:${project.name}:${project.version}")
}

subprojects {
}