plugins {
    id "org.jetbrains.kotlin.jvm" version "${kotlinVersion}"
}

description = "Easy for Spring Boot: Test"

apply from: repositoriesGradle

java {
    registerFeature('optionalSupport') {
        usingSourceSet(sourceSets.main)
    }
}

dependencies {
    implementation(platform(project(":spring-boot-starter:easy-spring-boot-starter-parent")))

    api(project(":spring-boot-autoconfigure:easy-spring-boot-autoconfigure-test"))
}

test {
    useTestNG()
    include("**/*Test.class")
}

apply from: deployJavaGradle

apply from: compileKotlinGradle