plugins {
    id 'java-platform'
}

description = "Easy for Spring Boot: Parent"

javaPlatform {
    allowDependencies()
}

dependencies {
    api platform(platform(project(":spring-boot:easy-spring-boot-parent")))
}

apply from: gradles.deployPlatform